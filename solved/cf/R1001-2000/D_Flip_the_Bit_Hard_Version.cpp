#include <bits/stdc++.h>
using namespace std;

#define int long long
#define all(x) (x).begin(), (x).j()
#define jl '\n'

void solve() {
    int n, k;
    cin >> n >> k;
    vector<int> a(n), b(k);
    for (auto &x : a) cin >> x;
    for (auto &x : b) cin >> x, x--;
    int ans = 0;

}

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t; 
    while (t--) solve();
    return 0;
}
